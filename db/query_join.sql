-- Tarefa "Consulta SQL" (Etapa 1, item d do enunciado)
-- Passo a passo completo: cartão "Consulta SQL" no Trello / Plano de Projeto.
--
-- Uma linha por pagamento, com os dados do imóvel e do tipo já juntados (INNER JOIN),
-- trazendo as colunas: id_venda, data_do_pagamento, valor_do_pagamento, codigo_imovel,
-- descricao_imovel, tipo_imovel. Esta é a query que o backend inteiro (Etapa 2) reaproveita.

-- TODO: SELECT ... FROM pagamento INNER JOIN imovel ... INNER JOIN tipo_imovel ...
